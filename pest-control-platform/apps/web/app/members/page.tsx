'use client';import {useEffect,useState} from 'react';import Shell from '../../components/Shell';import {api} from '../../lib/api';
export default function Members(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/users').then(setRows)},[]);return <Shell><div className="section-title"><div><h1 style={{margin:0}}>Members</h1><div className="muted">Role and field presence overview.</div></div></div><div className="card section"><table className="table"><thead><tr><th>Name</th><th>Username</th><th>Position</th><th>Role</th><th>Last Seen</th><th>Status</th></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td><b>{u.fullName}</b><div className="muted">{u.email}</div></td><td>{u.username}</td><td>{u.position||'-'}</td><td>{u.role.name}</td><td>{u.lastSeenAt?new Date(u.lastSeenAt).toLocaleString('en-ID'):'-'}</td><td><span className="badge green">{u.status}</span></td></tr>)}</tbody></table></div></Shell>}
