'use client';import {useEffect,useState} from 'react';import Shell from '../../components/Shell';import {api,API} from '../../lib/api';
export default function Reports(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/service-reports').then(setRows)},[]);return <Shell><div className="section-title"><div><h1 style={{margin:0}}>Service Reports</h1><div className="muted">Digital version of the two-page service report template.</div></div><a className="btn btn-primary" href="/service-reports/new">Create</a></div><div className="card section"><table className="table"><thead><tr><th>Report</th><th>Customer</th><th>Technician</th><th>Date</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.reportNumber}</b><div className="muted">{r.task.taskCode}</div></td><td>{r.customer.name}</td><td>{r.technician.fullName}</td><td>{new Date(r.date).toLocaleDateString('en-ID')}</td><td><span className={'badge '+(r.status==='APPROVED'?'green':r.status==='REJECTED'?'red':'orange')}>{r.status}</span></td><td><a className="btn btn-light" href={`${API}/service-reports/${r.id}/pdf`} target="_blank">PDF</a></td></tr>)}</tbody></table></div></Shell>}
