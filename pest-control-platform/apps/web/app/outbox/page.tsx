'use client';import Shell from '../../components/Shell';export default function Outbox(){return <Shell><h1>Outbox</h1><div className="card section stat"><div className="badge green">READY</div><h3>Offline sync foundation</h3><div className="muted">The API supports idempotent-oriented batch GPS ingestion. The mobile PWA queue should use IndexedDB in the next hardening phase; this UI intentionally does not fake a synced queue.</div></div></Shell>}
