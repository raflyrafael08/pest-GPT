'use client';import {MapContainer,TileLayer,Marker,Popup,Circle} from 'react-leaflet';import L from 'leaflet';import 'leaflet/dist/leaflet.css';
const icon=new L.Icon({iconUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png',iconRetinaUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png',shadowUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png',iconSize:[25,41],iconAnchor:[12,41]});
export default function LiveMap({users}:{users:any[]}){const valid=users.filter(u=>u.lastLatitude&&u.lastLongitude);const center=valid[0]?[Number(valid[0].lastLatitude),Number(valid[0].lastLongitude)]:[-6.2,106.8];return <div className="map"><MapContainer center={center as any} zoom={11} style={{height:'100%',width:'100%'}}><TileLayer attribution='&copy; OpenStreetMap contributors' url='https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png'/>{valid.map(u=><Marker key={u.id} position={[Number(u.lastLatitude),Number(u.lastLongitude)]} icon={icon}><Popup><b>{u.fullName}</b><br/>{u.position}<br/>Last update: {u.lastSeenAt?new Date(u.lastSeenAt).toLocaleString('en-ID'):'-'}</Popup><Circle center={[Number(u.lastLatitude),Number(u.lastLongitude)]} radius={20}/></Marker>)}</MapContainer></div>}
