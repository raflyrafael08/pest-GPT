'use client';
import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import {api} from '../../lib/api';
export default function Login(){const [username,setUsername]=useState('admin');const [password,setPassword]=useState('Admin123!');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({username,password})});router.replace('/dashboard')}catch(e:any){setError(e.message||'Login failed')}finally{setLoading(false)}}return <div className="auth"><form className="card auth-card" onSubmit={submit}><div className="brand"><div className="brand-mark">P</div><div><div style={{fontWeight:800,fontSize:20}}>Proteksi Pest Control</div><div className="muted" style={{fontSize:12}}>Field Operations Management Platform</div></div></div>{error&&<div className="card" style={{padding:12,background:'#fff0f0',color:'#9e2d2d',marginBottom:12}}>{error}</div>}<label className="label">Username / Email</label><input className="input" value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username"/><label className="label">Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/><button className="btn btn-primary full" style={{marginTop:18}} disabled={loading}>{loading?'Signing in…':'Login'}</button><div className="muted" style={{fontSize:12,marginTop:14}}>Demo admin: <b>admin / Admin123!</b><br/>Demo technician: <b>technician / Tech123!</b></div></form></div>}
