'use client'; import {useEffect,useState} from 'react'; import Shell from '../../components/Shell'; import {api} from '../../lib/api'; import Link from 'next/link';
export default function Tasks(){const [rows,setRows]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{api('/tasks').then(setRows).finally(()=>setLoading(false))},[]);return <Shell><div className="section-title"><div><h1 style={{margin:0}}>Tasks</h1><div className="muted">All operational work available to your account.</div></div><Link className="btn btn-primary" href="/service-reports/new">Create Service Report</Link></div><div className="card section" style={{padding:0}}>{loading?<div className="empty">Loading…</div>:<table className="table"><thead><tr><th>Task</th><th>Customer</th><th>Technician</th><th>Scheduled</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(t=><tr key={t.id}><td><b>{t.taskCode}</b><div className="muted">{t.title}</div></td><td>{t.customer.name}</td><td>{t.assignments?.map((a:any)=>a.user.fullName).join(', ')||'-'}</td><td>{t.scheduledStart?new Date(t.scheduledStart).toLocaleString('en-ID'): '-'}</td><td><span className={'badge '+(t.status==='COMPLETED'?'green':t.status==='OVERDUE'?'red':'orange')}>{t.status}</span></td><td><Link className="btn btn-light" href={`/tasks/${t.id}`}>Open</Link></td></tr>)}</tbody></table>}</div></Shell>}
